"""
URL routing for data_sync app.
"""

from django.urls import path

app_name = 'data_sync'

urlpatterns = [
    # TODO: Add sync endpoints (admin only)
    # path('tmdb/', views.TriggerTMDbSyncView.as_view(), name='sync-tmdb'),
]
